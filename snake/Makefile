
include $(APPDIR)/Make.defs
include $(SDKDIR)/Make.defs

PROGNAME = $(CONFIG_SNAKE_SNAKE_PROGNAME)
PRIORITY = $(CONFIG_SNAKE_SNAKE_PRIORITY)
STACKSIZE = $(CONFIG_SNAKE_SNAKE_STACKSIZE)
MODULE = $(CONFIG_SNAKE_SNAKE)

ASRCS =
CSRCS = 
MAINSRC = snake_main.c

include $(APPDIR)/Application.mk
